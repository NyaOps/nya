use nya::core::schema::{SchemaRegistry};

fn main() {
  // need to get context
  // need to get schema
  // need to register services/handlers
  // need to create event bus
  // need to trigger planner 
}